<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:localeConfig="@xml/locales_config"
        android:supportsRtl="true"
        android:theme="@style/Theme.EinkScreensaver">

        <!-- Настройки -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name="com.eink.screensaver.settings.SettingsActivity" android:exported="false" />
        <activity android:name="com.eink.screensaver.settings.ModuleSettingsActivity" android:exported="false" />
        <activity android:name="com.eink.screensaver.settings.ClockSettingsActivity" android:exported="false" />
        <activity android:name="com.eink.screensaver.settings.WeatherSettingsActivity" android:exported="false" />
        <activity android:name="com.eink.screensaver.settings.NewsSettingsActivity" android:exported="false" />
        <activity android:name="com.eink.screensaver.settings.NotesSettingsActivity" android:exported="false" />
        <activity android:name="com.eink.screensaver.settings.BookSettingsActivity" android:exported="false" />

        <!-- Скринсейвер поверх lockscreen -->
        <activity
            android:name=".LockScreenActivity"
            android:exported="false"
            android:theme="@style/Theme.LockScreen"
            android:showWhenLocked="true"
            android:excludeFromRecents="true"
            android:taskAffinity=".lockscreen"
            android:launchMode="singleTask"
            android:screenOrientation="portrait"
            android:configChanges="orientation|screenSize" />

        <!-- Сервис: SCREEN_OFF → WakeLock → fullScreenIntent → Activity -->
        <service
            android:name=".ScreenSaverService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="eink_screensaver_wakelock_fullscreen" />
        </service>

        <!-- Per-app language support (AppCompat auto-storage for pre-Android 13) -->
        <service
            android:name="androidx.appcompat.app.AppLocalesMetadataHolderService"
            android:enabled="false"
            android:exported="false">
            <meta-data
                android:name="autoStoreLocales"
                android:value="true" />
        </service>

        <!-- Единственная задача — вернуть устройство в сон после пробуждения
             с вендорным тегом; см. SleepAccessibilityService. -->
        <service
            android:name=".SleepAccessibilityService"
            android:exported="true"
            android:label="@string/accessibility_service_label"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <!-- Автозапуск при загрузке -->
        <receiver
            android:name=".BootReceiver"
            android:exported="true"
            android:enabled="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

    </application>
</manifest>
