<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/touchInterceptor"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#FFFFFF"
    android:clickable="true"
    android:focusable="true">

    <ImageView
        android:id="@+id/backgroundImage"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="fitCenter"
        android:visibility="gone" />

    <LinearLayout
        android:id="@+id/mainContainer"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:paddingStart="20dp"
        android:paddingEnd="20dp"
        android:paddingTop="32dp"
        android:paddingBottom="80dp">

        <!-- Header row: clock on the left, weather summary on the right.
             This whole row is the "clock" module slot in reorderModules(); the
             weather summary, forecast strip included, lives inside it. -->
        <LinearLayout
            android:id="@+id/headerRow"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:baselineAligned="false"
            android:layout_marginBottom="16dp">

            <LinearLayout
                android:id="@+id/clockSection"
                android:layout_width="0dp"
                android:layout_weight="1"
                android:layout_height="wrap_content"
                android:orientation="vertical">

                <TextView
                    android:id="@+id/clockText"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="00:00"
                    android:textSize="60sp"
                    android:textColor="#000000"
                    android:fontFamily="sans-serif-light"
                    android:letterSpacing="0.05"
                    android:includeFontPadding="false" />

                <TextView
                    android:id="@+id/dateText"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text=""
                    android:textSize="16sp"
                    android:textColor="#333333"
                    android:fontFamily="sans-serif"
                    android:layout_marginTop="4dp"
                    android:includeFontPadding="false" />
            </LinearLayout>

            <LinearLayout
                android:id="@+id/weatherSummary"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:gravity="end"
                android:layout_marginStart="12dp"
                android:visibility="gone">

                <TextView
                    android:id="@+id/weatherCurrentText"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:textSize="16sp"
                    android:textColor="#000000"
                    android:gravity="end"
                    android:fontFamily="sans-serif" />

                <TextView
                    android:id="@+id/weatherDayNightText"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:textSize="13sp"
                    android:textColor="#333333"
                    android:gravity="end"
                    android:fontFamily="sans-serif"
                    android:layout_marginTop="2dp" />

                <!-- Forecast slots live in the corner too, built in
                     buildForecastSlot() at a deliberately small size. -->
                <LinearLayout
                    android:id="@+id/weatherForecastRow"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:orientation="horizontal"
                    android:baselineAligned="false"
                    android:layout_marginTop="4dp" />
            </LinearLayout>
        </LinearLayout>


        <!-- News section -->
        <FrameLayout
            android:id="@+id/newsSection"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="12dp"
            android:clipChildren="false"
            android:clipToPadding="false"
            android:background="@drawable/section_border"
            android:padding="10dp"
            android:visibility="gone">

            <TextView
                android:id="@+id/newsText"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="16dp"
                android:textSize="13sp"
                android:textColor="#000000"
                android:fontFamily="sans-serif"
                android:lineSpacingMultiplier="1.3" />

            <TextView
                android:id="@+id/newsLabel"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/label_news"
                android:textSize="12sp"
                android:textColor="#888888"
                android:fontFamily="sans-serif" />
        </FrameLayout>

        <!-- Stickers section (two-column grid, built programmatically) -->
        <LinearLayout
            android:id="@+id/stickersSection"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:layout_marginBottom="12dp"
            android:visibility="gone" />

        <!-- Book section -->
        <FrameLayout
            android:id="@+id/bookSection"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:clipChildren="false"
            android:clipToPadding="false"
            android:background="@drawable/section_border"
            android:padding="10dp"
            android:visibility="gone">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:orientation="horizontal"
                android:layout_marginTop="16dp">

                <ImageView
                    android:id="@+id/bookCoverImage"
                    android:layout_width="144dp"
                    android:layout_height="match_parent"
                    android:scaleType="centerCrop"
                    android:layout_marginEnd="12dp" />

                <LinearLayout
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:orientation="vertical">

                    <TextView
                        android:id="@+id/bookTitleText"
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:textSize="13sp"
                        android:textColor="#000000"
                        android:textStyle="bold"
                        android:fontFamily="sans-serif" />

                    <TextView
                        android:id="@+id/bookAuthorText"
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:textSize="12sp"
                        android:textColor="#333333"
                        android:fontFamily="sans-serif"
                        android:layout_marginTop="2dp" />

                    <TextView
                        android:id="@+id/bookAnnotationText"
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:textSize="11sp"
                        android:textColor="#555555"
                        android:fontFamily="sans-serif"
                        android:layout_marginTop="2dp" />
                </LinearLayout>
            </LinearLayout>

            <TextView
                android:id="@+id/bookLabel"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/label_book"
                android:textSize="12sp"
                android:textColor="#888888"
                android:fontFamily="sans-serif" />
        </FrameLayout>

    </LinearLayout>

    <!-- Unlock hint (fixed at bottom, not affected by anti-ghosting) -->
    <TextView
        android:id="@+id/unlockHintText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|center_horizontal"
        android:layout_marginBottom="24dp"
        android:text="@string/unlock_hint"
        android:textSize="11sp"
        android:textColor="#AAAAAA"
        android:gravity="center" />

</FrameLayout>
